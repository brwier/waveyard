# Issue tracker: GitHub

Issues and specs for this repo live as GitHub issues on `brwier/waveyard`.
Use the `gh` CLI for all operations.

This file follows the GitHub template from the `setup-matt-pocock-skills`
skill; consult that skill's `issue-tracker-github.md` for the full list of
operations (create, read, list, comment, label, close) and for the
wayfinding operations (map, child ticket, blocking via native issue
dependencies, frontier query, claim, resolve).

**PRs as a request surface: no.**
